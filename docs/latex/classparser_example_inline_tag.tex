\hypertarget{classparser_example_inline_tag}{\section{parser\-Example\-Inline\-Tag \-Class \-Reference}
\label{classparser_example_inline_tag}\index{parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}}
}
\-Inheritance diagram for parser\-Example\-Inline\-Tag\-:\begin{figure}[H]
\begin{center}
\leavevmode
\includegraphics[height=4.000000cm]{classparser_example_inline_tag}
\end{center}
\end{figure}
\subsection*{\-Public \-Member \-Functions}
\begin{DoxyCompactItemize}
\item 
\hyperlink{classparser_example_inline_tag_a8d152e374dbf66e8adb6cb2b8a7cd11f}{parser\-Example\-Inline\-Tag} (\$value, \$current\-\_\-path, \$is\-Tutorial=false)
\item 
\hyperlink{classparser_example_inline_tag_ae0f8c008fd80913cb81e2dc4ee0d6dc6}{set\-Source} (\$source, \$class=false)
\item 
\hyperlink{classparser_example_inline_tag_ad6a7a831f5306b7129955f7bcdcb7173}{array\-Convert} (\&\$c)
\item 
\hyperlink{classparser_example_inline_tag_ac1eeb3b2c221c88d9444847da4ed9d8d}{get\-Program\-Listing} ()
\end{DoxyCompactItemize}


\subsection{\-Detailed \-Description}


\-Definition at line 568 of file \-Inline\-Tags.\-inc.



\subsection{\-Member \-Function \-Documentation}
\hypertarget{classparser_example_inline_tag_ad6a7a831f5306b7129955f7bcdcb7173}{\index{parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}!array\-Convert@{array\-Convert}}
\index{array\-Convert@{array\-Convert}!parserExampleInlineTag@{parser\-Example\-Inline\-Tag}}
\subsubsection[{array\-Convert}]{\setlength{\rightskip}{0pt plus 5cm}{\bf array\-Convert} (
\begin{DoxyParamCaption}
\item[{\&\$}]{c}
\end{DoxyParamCaption}
)}}\label{classparser_example_inline_tag_ad6a7a831f5306b7129955f7bcdcb7173}
converter helper for \-P\-H\-P 4.\-3.\-0+


\begin{DoxyParams}[1]{\-Parameters}
\hyperlink{class_converter}{\-Converter} & {\em \&\$c} & output converter\\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{\-Returns}
string  \hyperlink{classphp_documentor___highlight_parser}{php\-Documentor\-\_\-\-Highlight\-Parser} \-Parses the tokenized source 
\end{DoxyReturn}


\-Reimplemented from \hyperlink{classparser_source_inline_tag_ad6a7a831f5306b7129955f7bcdcb7173}{parser\-Source\-Inline\-Tag}.



\-Definition at line 734 of file \-Inline\-Tags.\-inc.


\begin{DoxyCode}
    {
        $source = $this->source;
        if ($this->end != '*') {
            $source = array_slice($this->source, 0, $this->end + $this->start -
       1);
        }
        $start = $this->start - 1;
        if ($start < 0) {
            $start = 0;
        }
        return $c->exampleProgramExample($source, true, true, $this->_class, 
      $start);
    }
\end{DoxyCode}
\hypertarget{classparser_example_inline_tag_ac1eeb3b2c221c88d9444847da4ed9d8d}{\index{parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}!get\-Program\-Listing@{get\-Program\-Listing}}
\index{get\-Program\-Listing@{get\-Program\-Listing}!parserExampleInlineTag@{parser\-Example\-Inline\-Tag}}
\subsubsection[{get\-Program\-Listing}]{\setlength{\rightskip}{0pt plus 5cm}{\bf get\-Program\-Listing} (
\begin{DoxyParamCaption}
{}
\end{DoxyParamCaption}
)}}\label{classparser_example_inline_tag_ac1eeb3b2c221c88d9444847da4ed9d8d}
\-Return the source for the example file, enclosed in a $<$programlisting$>$ tag to use in a tutorial

\begin{DoxyReturn}{\-Returns}
string 
\end{DoxyReturn}


\-Definition at line 753 of file \-Inline\-Tags.\-inc.


\begin{DoxyCode}
    {
        $source = explode("\n", $this->source);
        $start  = $this->start;
        if ($this->end != '*') {
            $source = array_slice($source, $start - 1, $this->end - $start + 1)
      ;
        } else {
            $source = array_slice($source, $start - 1);
        }
        $source = join("\n", $source);
        return
        "<programlisting role=\"php\">
         <![CDATA[\n" .
          $source .
        "\n]]>\n</programlisting>";
    }
\end{DoxyCode}
\hypertarget{classparser_example_inline_tag_a8d152e374dbf66e8adb6cb2b8a7cd11f}{\index{parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}!parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}}
\index{parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}!parserExampleInlineTag@{parser\-Example\-Inline\-Tag}}
\subsubsection[{parser\-Example\-Inline\-Tag}]{\setlength{\rightskip}{0pt plus 5cm}{\bf parser\-Example\-Inline\-Tag} (
\begin{DoxyParamCaption}
\item[{\$}]{value, }
\item[{\$}]{current\-\_\-path, }
\item[{\$}]{is\-Tutorial = {\ttfamily false}}
\end{DoxyParamCaption}
)}}\label{classparser_example_inline_tag_a8d152e374dbf66e8adb6cb2b8a7cd11f}
constructor


\begin{DoxyParams}[1]{\-Parameters}
string & {\em \$value} & format \char`\"{}filepath\mbox{[} start \mbox{[}end\mbox{]}\mbox{]}\char`\"{} where start and end are line numbers with the end line number optional \\
\hline
string & {\em \$current\-\_\-path} & full path to the current file, used to check relative directory locations \\
\hline
bool & {\em \$is\-Tutorial} & if true, then this is in a tutorial\\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{\-Returns}
mixed 
\end{DoxyReturn}
\begin{DoxyRefDesc}{\-Todo}
\item[\hyperlink{todo__todo000116}{\-Todo}]replace tokenizer\-\_\-ext constant with \-T\-O\-K\-E\-N\-I\-Z\-E\-R\-\_\-\-E\-X\-T for \-C\-S rule \end{DoxyRefDesc}


\-Definition at line 583 of file \-Inline\-Tags.\-inc.


\begin{DoxyCode}
    {
        global $_phpDocumentor_setting;
        parserInlineTag::parserInlineTag('example', '');
        $path     = false;
        $tagValue = trim($value);
        $path     = $isAbsPath = $pathOnly = $fileName = $fileExt 
            = $original_path  = $title = false;
        do {
            // make sure the format is stuff.ext startline[ endline]
            if (!preg_match('`(.*)\.(\w*)\s(.*)`', $tagValue, $match)) {
                // or format is stuff.ext
                if (!preg_match('`(.*)\.(\w*)\s*$`', $tagValue, $match)) {
                    // Murphy: Some funny path was given
                    $original_path = $tagValue; // used for error output
                    break; // try-block
                }
            }
            if (strlen($match[1]) === 0) {
                // Murphy: Some funny path was given
                $original_path = $tagValue; // used for error output
                break; // try-block
            }
            $fileExt = $match[2];
            if (isset($match[3])) {
                $lines       = explode(' ', trim($match[3]));
                $this->start = (int) $lines[0];
                if (isset($lines[1])) {
                    $this->end = (int) $lines[1];
                }
            }
            // Replace windows '\' the path.
            $pathTmp = str_replace('\\', '/', $match[1]);

            // Is there a path and a file or is it just a file?
            if (strpos($pathTmp, '/') === false) {
                // No path part
                $pathOnly = '';
                $fileName = $pathTmp .'.'. $fileExt;
            } else {
                // split the path on the last directory, find the filename
                $splitPos = strrpos($pathTmp, '/');
                $pathOnly = substr($match[1], 0, $splitPos+1);
                $fileName = substr($match[1], $splitPos+1) .'.'. $fileExt;
                // Is the path absolute? (i.e. does it start like an absolute
       path?)
                if (('/' === $pathTmp[0]) || preg_match('`^\w*:`i', $pathTmp)) 
      {
                    // works for both windows 'C:' and URLs like 'http://'
                    $isAbsPath = true; // Yes
                }
            }

            $original_path = $pathOnly . $fileName;

            // Now look for the file starting with abs. path.
            if ($isAbsPath) {
                // remove any weirdities like /../file.ext
                $tmp = realpath($original_path);
                if ($tmp && is_file($tmp)) {
                    $path = $tmp;
                }
                /*
                 * Alway break if abs. path was detected,
                 * even if file was not found.
                 */
                break; // try-block
            }

            // Search for the example file some standard places 
            // 1) Look if the ini-var examplesdir is set and look there ...
            if (isset($_phpDocumentor_setting['examplesdir'])) {
                $tmp = realpath($_phpDocumentor_setting['examplesdir'] 
                    . PATH_DELIMITER  . $original_path);
                if ($tmp && is_file($tmp)) {
                    $path = $tmp; // Yo! found it :)
                    break; // try-block
                }
            }

            // 2) Then try to look for an 'example/'-dir 
            //    below the *currently* parsed file ...
            if (!empty($current_path)) {
                $tmp = realpath(dirname($current_path) . PATH_DELIMITER . '
      examples'
                    . PATH_DELIMITER . $fileName);
                if ($tmp && is_file($tmp)) {
                    $path = $tmp; // Yo! found it :)
                    break; // try-block
                }
            }

            // 3) Then try to look for the example file 
            //    below the subdir PHPDOCUMENTOR_BASE/examples/ ...
            if (is_dir(PHPDOCUMENTOR_BASE . PATH_DELIMITER . 'examples')) {
                $tmp = realpath(PHPDOCUMENTOR_BASE . PATH_DELIMITER . 'examples
      ' 
                    . PATH_DELIMITER . $original_path);
                if ($tmp && is_file($tmp)) {
                    $path = $tmp; // Yo! found it :)
                    break; // try-block
                }
            }

            $tmp = realpath(PHPDOCUMENTOR_BASE . PATH_DELIMITER . 
      $original_path);
            if ($tmp && is_file($tmp)) {
                $path = $tmp; // Yo! found it :)
                break; // try-block
            }
            // If we reach this point, nothing was found and $path is false.
        } while (false);

        if (!$path) {
            addWarning(PDERROR_EXAMPLE_NOT_FOUND, $original_path);
            $this->path = false;
        } else {
            $f = @fopen($path, 'r');
            if ($f) {
                $example = fread($f, filesize($path));
                if (tokenizer_ext && !$isTutorial) {
                    $obj = new phpDocumentorTWordParser;
                    $obj->setup($example);
                    $this->setSource($obj->getFileSource());
                    unset($obj);
                } else {
                    $this->setSource($example);
                }
            }
        }
    }
\end{DoxyCode}
\hypertarget{classparser_example_inline_tag_ae0f8c008fd80913cb81e2dc4ee0d6dc6}{\index{parser\-Example\-Inline\-Tag@{parser\-Example\-Inline\-Tag}!set\-Source@{set\-Source}}
\index{set\-Source@{set\-Source}!parserExampleInlineTag@{parser\-Example\-Inline\-Tag}}
\subsubsection[{set\-Source}]{\setlength{\rightskip}{0pt plus 5cm}{\bf set\-Source} (
\begin{DoxyParamCaption}
\item[{\$}]{source, }
\item[{\$}]{class = {\ttfamily false}}
\end{DoxyParamCaption}
)}}\label{classparser_example_inline_tag_ae0f8c008fd80913cb81e2dc4ee0d6dc6}
sets the source


\begin{DoxyParams}[1]{\-Parameters}
string | array & {\em \$source} & source code \\
\hline
string | bool & {\em \$class} & class name if this is a method, boolean in php 4.\-3.\-0, if this is a method this will be true\\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{\-Returns}
void 
\end{DoxyReturn}


\-Reimplemented from \hyperlink{classparser_source_inline_tag_ae0f8c008fd80913cb81e2dc4ee0d6dc6}{parser\-Source\-Inline\-Tag}.



\-Definition at line 720 of file \-Inline\-Tags.\-inc.


\begin{DoxyCode}
    {
        $this->_class = $class;
        $this->source = $source;
    }
\end{DoxyCode}


\-The documentation for this class was generated from the following file\-:\begin{DoxyCompactItemize}
\item 
\-C\-:/wamp/www/\-Mitos\-E\-H\-R/lib/\-Php\-Documentor/php\-Documentor/\hyperlink{_inline_tags_8inc}{\-Inline\-Tags.\-inc}\end{DoxyCompactItemize}
